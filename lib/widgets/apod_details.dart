import 'package:flutter/material.dart';

class ApodDetails extends StatelessWidget {
  const ApodDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
